# ==============================================================================
# CallShadow Backend — Python Dependencies
# ==============================================================================
#
# IMPORTANT: torch and torchaudio are NOT listed here because they require a
# special CPU build from the PyTorch index and conflict with PyPI resolution.
#
# Install them FIRST, before this file:
#   pip install torch==2.6.0+cpu torchaudio==2.6.0+cpu \
#       --index-url https://download.pytorch.org/whl/cpu
#
# Then install everything else:
#   pip install -r requirements.txt
#
# pip-audit: torch/torchaudio will appear as "skipped" because they are not
# published on PyPI. This is expected and not a vulnerability indicator.
# ==============================================================================

# --- ML / Audio ---
transformers~=5.16.1
scikit-learn~=1.9.0
speechbrain~=1.1.1
soundfile~=0.14.0
librosa~=1.0.0
scipy~=1.18.1
numpy~=2.5.2
imageio-ffmpeg~=0.6.0

# --- Web Framework ---
fastapi~=0.141.1
uvicorn[standard]~=0.52.4
websockets~=17.1
pydantic~=2.13.4
pydantic-settings~=2.15.0
slowapi~=0.1.10
httpx~=0.28.1
aiofiles~=23.1.0

# --- Database ---
sqlalchemy~=2.0.54

# --- PDF / Reporting ---
reportlab~=5.0.1
