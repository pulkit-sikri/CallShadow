# ==============================================================================
# CallShadow Web Frontend Configuration (.env)
# ==============================================================================

# If served by backend statically, leave API_BASE_URL empty in js/config.js
# If hosted on a separate static server/CDN, point to the FastAPI backend URL:
VITE_API_BASE_URL=http://localhost:8000
